import React from 'react'
import Layout from 'components/Layout'

// type Props = {
//   value: string;
// }

const Tag = (props: any) => {
  console.log(props)
  return <Layout>
    tag
  </Layout>
}

export default Tag